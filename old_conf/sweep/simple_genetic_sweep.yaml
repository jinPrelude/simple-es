program: /home/euijin/Documents/give-life-to-agents/run_es.py
method: bayes
metric: 
  name: ep5_mean_reward
  goal: maximize
parameters:
  elite_num:
    max: 20
    min: 1
  init_sigma:
    max: 4.0
    min: 1.0
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens} 